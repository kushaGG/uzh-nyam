<nav class="navbar navbar-default">

  <div class="container">

    <div class="navbar-header">
      <%= link_to image_tag("/assets/logo1.png"), root_path %>
    </div>
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
         <li><%= link_to 'Check new institutions', catalogs_path %></li>
        <% end %>
      <% end %>

      <%= form_tag search_catalogs_path, method: :get do |f| %>
        <%= text_field_tag :search, nil, :placeholder => "search" %>
        <%= submit_tag "Search" %>
      <% end %>
    </ul>

      <% if user_signed_in? %>
      <ul class="dropdown">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-danger" data-toggle="dropdown" href="#"><%= image_tag(current_user.avatar_url, size:"40x40", class:"rounded-circle") %>
            <%= current_user.username %></a>
          <div class="dropdown-menu">

          <a class="dropdown-item2"><%= link_to "Категорії",admin_categories_path, class: "nav-link text-danger" %></a>
          <a class="dropdown-item1"><%= link_to "Вийти", destroy_user_session_path,class: "nav-link text-danger", method: :delete %></a><br />
            </div>

        </li>
        </ul>
      <% else %>
        <li class="login"><%= link_to "Ввійти", new_user_session_path,class: "nav-link text-danger"%></li>
      <% end %>
  </div>
</nav>
