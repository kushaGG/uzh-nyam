
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <div class="container-fluid">
    <div class="navbar-header">
      <li><%= link_to "Home", root_path, :class => 'navbar-brand'  %></li>
    </div>
    <ul class="navbar-nav">

      <li></li>
    </ul>
    <ul class="navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="nav-link">  <strong>
          <%= current_user.email.split('@').first %></strong>
        <%= image_tag(current_user.avatar_url, size: "30x30") %></li>
        <%= link_to 'Edit profile', edit_user_registration_path%> |
        <%= link_to "Logout", destroy_user_session_path, method: :delete %>
      <% else %>
        <li><%= link_to  new_user_session_path, :class => 'nav-link' do %>Login
          <i class="fas fa-sign-in-alt"></i>
        <% end %></li>
      <% end %>
    </ul>
  </div>
</nav>
